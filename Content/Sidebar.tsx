import React from "react";
import { setNavOpen } from "@/Store/Store";
import { useSelector, useDispatch } from "react-redux";

export const Sidebar = () => {
  //   const isNavOpen = useSelector(() => state.app.isNavOpen)
  return <div>Sidebar</div>;
};
